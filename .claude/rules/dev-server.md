## ローカル開発サーバーの利用ルール

### 原則

- **ユーザーが事前に起動したローカル開発サーバーを必ず利用すること**
- 既にユーザーが起動しているローカル開発サーバー（localhost）が存在する場合は、
  **新たにサーバーを起動せず、必ずそのポートを利用すること**

### 想定例

- 既に `http://localhost:3000` が起動している → そのまま使用する
- Playwright / MCP による E2E 検証も、既存の起動済みポートを対象に行う