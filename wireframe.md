# Wireframe Specification - Beer Salon

モバイルファーストで設計した画面レイアウトのテキストワイヤーフレーム。  
PC では横幅に応じて余白や2カラムレイアウトにすることを想定。

---

## 0. 共通コンポーネント

### 0-1. ログイン前レイアウト

- ヘッダー: なし（または簡易ロゴのみ）
- コンテンツ: 中央寄せフォーム
- フッター: コピーライト簡易表記のみ（任意）

### 0-2. ログイン後レイアウト（Dark Taproom）

配色・タイポは Dark Taproom 基盤（`design_handoff_user_screens/README.md` の Design Tokens が正）に準拠する。共通レイアウトは `apps/web/src/components/layout/`（`Header` / `Footer` / `HeaderNav` / `AuthenticatedLayout` / `nav-items.ts`）で実装し、`AuthenticatedLayout` を使う全ページに一括反映される。ナビ項目は `nav-items.ts` に集約し、PC 横ナビ（Header）とモバイルボトムナビ（Footer）で共有する。

- **共通ヘッダー（画面上部固定・全幅共通）**
  - 左: サービスロゴ（クリックで `/`）。Dark Taproom 準拠の案A（`apps/web/src/components/layout/beer-salon-logo.tsx`・アンバータイル + 泡付きグラス + ワードマーク「Beer Salon」）。ワードマークのフォント（明朝 `--font-mincho` / Archivo `--font-archivo`）を確実に適用するためインライン SVG で描画する（外部 `<img src>` だと `next/font` が効かずフォールバックするため）。ブラウザタブの favicon は同シンボルを切り出した `apps/web/src/app/icon.svg`（Next.js App Router 規約）
  - 右: アイコン群（各アイコンは `#241a0e`（`surface-raised`）の丸背景 + アンバー薄ボーダー `border-primary/20`）
    - 通知アイコン（ベル・未読があれば赤バッジ。99 超は「99+」表示）
    - マイページアイコン（人型）
    - ログアウト（アイコンボタン）

- **PC（`md:` 以上）ヘッダー横ナビ**
  - ロゴとアイコン群の間に横ナビ（お気に入り / タイムライン / 閲覧履歴）+ 「投稿する」プライマリボタン（アンバーグラデ `from-primary`〜`to-primary-strong`）を表示
  - 現在ページのナビ項目はアンバー文字 + 下線でアクティブ表現（`aria-current="page"`）
  - PC ではボトムナビは表示しない（下部固定ナビの役割をヘッダー横ナビが担う）

- **モバイル（`md` 未満）共通フッター（画面下部固定・`md:hidden`）**
  - ボトムナビ 4 アイコン（等間隔）
    - お気に入りバー（★）→ `/favorites/bars`
    - タイムライン → `/timeline`
    - 投稿（＋）→ `/posts/new`（**中央 FAB**。アンバーグラデ丸 `from-primary`〜`to-primary-strong`・一段浮かせて表示）
    - 閲覧履歴（🕒）→ `/history/bars`
  - アクティブ項目はアンバー（`text-primary` / `fill-primary`・`aria-current="page"`）、非アクティブは `subtext`

- **コンテンツエリア**
  - ヘッダー下（`pt-16`）〜フッター上でスクロール。PC はボトムナビが無いため下部パディングを詰める（`pb-16 md:pb-0`）

- **認証画面（`/login`・`/signup` 等）**: `AuthenticatedLayout` を使用しないため、共通ヘッダー/フッターは表示されない

---

## 1. 認証系画面

### 1-1. ログインページ `/login`

Dark Taproom（11a モバイル / 11c PC スプリット）で実装（#447）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。`AuthenticatedLayout` を使わないため共通ヘッダー/フッターは表示されない。

**レイアウト（モバイル / 11a）**

- 上部: ブランドヒーロー（`surface-raised`〜`surface-deep` グラデ + アンバー斜めストライプ + 発光円）
  - オーバーライン「Shizuoka Craft Beer」（Archivo・`uppercase`）＋ロゴ（明朝 `font-mincho`「Beer」＋ Archivo 太字「Salon」）＋タグライン「今夜の一杯を、この街で見つける。」（`font-mincho`）
- 中央: ログインフォームカード（`bg-card`・角丸・見出し「おかえりなさい」明朝）
  - ラベル「メールアドレス」（`subtext`）＋テキストボックス（`surface-raised` + アンバー薄ボーダー・プレースホルダーなし）
  - ラベル「パスワード」＋パスワード入力フィールド。右端に**表示トグル**（lucide `Eye`/`EyeOff`。押下で `type` を password ⇄ text 切替）
  - [ログイン] ボタン（幅いっぱい・アンバーグラデ `from-primary`〜`to-primary-strong`）
  - エラーメッセージエリア（バリデーション/認証失敗時のみ表示。`FormError`）
- 下部リンク:
  - 「新規登録はこちら」（アンバー）→ `/signup`
  - 「パスワードをお忘れの方」（`subtext`）→ `/password/forgot`

**レイアウト（PC / 11c）**

- `md:` 以上でカードを左右 2 カラムのスプリットにする
  - 左（400px）: ブランドビジュアル（ストライプ + 発光 + ロゴ + オーバーライン + 明朝コピー、左寄せ）
  - 右: フォーム（見出し「おかえりなさい」+ 補足文 + 上記フォーム）

### 1-2. 新規登録ページ `/signup`

Dark Taproom（11b）で実装（#447）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。`AuthenticatedLayout` を使わないため共通ヘッダー/フッターは表示されない。

**レイアウト**

- 上部: オーバーライン「Shizuoka Craft Beer」（Archivo・`uppercase`・アンバー）＋タイトル「新規登録」（明朝 `font-mincho`・中央揃え）
- 本文: フォームカード（`bg-card`・角丸）内の縦並びフォーム

1. メールアドレス
   - テキストボックス（`surface-raised` + アンバー薄ボーダー・プレースホルダーなし）
2. パスワード
   - パスワード入力 + 表示トグル（lucide `Eye`/`EyeOff`）
   - **強度メーター**（4分割バー。満たした条件数だけアンバー `--primary` 塗り、未達は `--secondary`）
     - 判定基準は `signUpSchema` と揃える（8文字以上・小文字・大文字・数字の4条件を各1段）。算出は純関数 `calcPasswordStrength`（`lib/auth/password-strength.ts`）
   - 強度ヒント: 「8文字以上、大文字・小文字・数字を含めてください」
3. [登録] ボタン（プライマリ・アンバーグラデ）
4. 「または」区切り + SNS登録ボタン（縦並び）
   - [Googleで登録] / [Xで登録]
   - **OAuth は本 Issue のスコープ外（別 Issue）。デザインの配置のみ再現し、動作は持たせず `disabled`（非活性）で表示する**
5. 「すでにアカウントをお持ちの方」リンク（アンバー）→ `/login`

- 登録完了後（`?success=true`）は同ページで完了画面を表示（アンバー丸のメールアイコン + 明朝見出し「確認メールを送信しました」+ 補足文）

### 1-3. プロフィール入力ページ `/signup/profile`

**レイアウト**

- タイトル: 「プロフィール入力」
- ステップインジケータ（任意）
  - 例: 「1/2 アカウント情報 → 2/2 プロフィール」

**フォーム項目**

- 姓（テキスト）
- 名（テキスト）
- ニックネーム（テキスト）
- 生年月日（年月日プルダウン）
- 性別（プルダウン: 男性 / 女性 / その他 / 無回答 など）
- お住まいの都道府県（プルダウン）

**下部**

- [戻る]（任意）
- [登録内容を確認] ボタン → `/signup/confirm`

### 1-4. 登録内容確認ページ `/signup/confirm`

**レイアウト**

- タイトル: 「登録内容の確認」
- 内容表示（読み取り専用）

  | 項目             | 値（テキスト）        |
  |------------------|-----------------------|
  | 姓               | 例: 下井田            |
  | 名               | 例: 陸                |
  | ニックネーム     | 例: りく              |
  | 生年月日         | 1994/05/13           |
  | 性別             | 男性                  |
  | お住まいの都道府県 | 静岡県               |

- 下部ボタン
  - [修正する] → `/signup/profile`
  - [この内容で登録する]（プライマリ）→ `/` へ遷移

### 1-5. パスワード再設定申請ページ `/password/forgot`

**レイアウト**

- 上部: サービスロゴ「Beer Salon」＋サブタイトル「パスワード再設定」
- 中央: 申請フォームカード
  - 補足文: 「ご登録のメールアドレスを入力してください。パスワード再設定用のリンクをお送りします。」
  - ラベル「メールアドレス」
    - テキストボックス（プレースホルダーなし。認証系フォームで統一）
  - エラーメッセージエリア（バリデーション失敗時のみ表示）
  - [再設定メールを送信] ボタン（幅いっぱい・プライマリ）
- 下部リンク:
  - 「ログインに戻る」 → `/login`

**振る舞い**

- 送信完了時: 同一ページ上に Sonner トーストで完了メッセージを表示
  - 文言: 「該当アドレスが登録されていればメールを送信しました」（登録有無を区別しない）
- クエリ `?error=invalid_token` 受信時: エラートースト「パスワード再設定リンクが無効です。再度メールアドレスを入力してください。」
- クエリ `?error=session_expired` 受信時: エラートースト「パスワード再設定の有効期限が切れました。再度メールアドレスを入力してください。」

### 1-6. パスワード再設定ページ `/password/reset`

**レイアウト**

- 上部: サービスロゴ「Beer Salon」＋サブタイトル「パスワード再設定」
- 中央: 再設定フォームカード
  - 補足文: 「新しいパスワードを入力してください。」
  - 新しいパスワード（パスワード入力）
    - 強度ヒント: 「8文字以上、大文字・小文字・数字を含めてください」
  - 新しいパスワード（確認用）（パスワード入力）
  - エラーメッセージエリア（バリデーション失敗・更新失敗時のみ表示）
  - [パスワードを変更する] ボタン（幅いっぱい・プライマリ）

**前提**

- Supabase Auth のリカバリーセッションが確立した状態（`/auth/callback?type=recovery` 経由）でのみアクセス可能
- リカバリーセッションが無い場合は `/password/forgot?error=session_expired` にリダイレクトされる

**振る舞い**

- 更新成功時: 自動的にサインアウトし、`/login?reset=success` にリダイレクト
- `/login?reset=success` 到達時: ログイン画面上に Sonner トースト「パスワードを再設定しました。新しいパスワードでログインしてください」を表示

---

## 2. トップ・検索・店舗関連

### 2-1. トップページ（検索ページ） `/`

Dark Taproom（1a / 2a）で実装（#442）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。

**レイアウト（モバイル / 1a）**

上から順に:

0. **ヒーロー**
   - オーバーライン（Archivo・`uppercase`・広めの `letter-spacing`・ラテン文字限定）＋明朝見出し「今夜の一杯を、この街で見つける。」（`font-mincho`）＋補足文

1. **検索セクション**
   - 検索バー（テキストボックス + アンバー丸検索ボタン（lucide `Search`。絵文字は廃止））
   - 市町村（ドロップダウン）
   - 「ビールカテゴリ」（チップ形式の複数選択。選択中は `#e0a341` 塗り + `✓`、未選択は `#2c2013` + 薄ボーダー。選択したカテゴリのいずれかを提供する店舗を OR 表示）
   - 産地（チップ形式の複数選択。既定は「産地で絞り込む」トグル（lucide `ChevronDown`・`aria-expanded`）で**折りたたみ**、展開すると国名見出しごとに地域チップをグルーピング表示し、その国に属する地域が一目で分かる。折りたたみ時も選択中の産地は地域名のサマリチップとして常に見え、タップで解除できる。選択中は `#e0a341` 塗り + `✓`、未選択は薄ボーダー（カテゴリと同一スタイル）。選択した産地のいずれかのビールを提供する店舗を OR 表示。カテゴリと産地の AND は店舗単位で判定し、別々のビールで各条件を満たしてよい。既定折りたたみは、DB 登録産地の増加で検索カードが縦に伸びファーストビュー（地図・店舗カード）を圧迫するのを防ぐため。Issue #491 / #497）
   - 検索状態（q / city / categories / origins）は URL クエリ（`q` / `city` / `cat`（カンマ区切り）/ `origin`（`国/地域` ペアのカンマ区切り））と同期し、ブラウザバックで復元される（真実の源は URL）

2. **地図エリア**
   - 高さ: 画面高さの 30〜40% 程度
   - Google Maps 埋め込み
   - ピン表示（店舗位置）
     - ピンは下部の店舗一覧と同一のデータソース（DB の `bars`。`getBars()` の検索結果）から描画し、一覧とマップが常にリンクする
     - 緯度経度（`bars.latitude` / `bars.longitude`）が登録された店舗のみピン化する（未登録の店舗は一覧には出るがピンは表示しない）
     - マップの中心・ズームは表示中のピン群に合わせて自動調整する
       - ピンが複数ある場合: 全ピンを内包するよう `fitBounds` で中心とズームを自動計算する（市町村代表座標に固定せず、ピンが画面外にはみ出さない）
       - ピンが1件のみの場合: そのピンを中心に据える（1点で `fitBounds` すると極端にズームインするため）
       - ピンが0件の場合: `fitBounds` の対象がないため、市町村選択時はその市町村の代表座標、未選択時は現在地へ寄せる
     - ピンをタップすると店舗名・所在地と「店舗詳細を見る」導線（`/bars/[barId]`）が出る

3. **店舗一覧コンポーネント**
   - カードリスト形式
   - 各カード（`bg-card` / 16px 角丸 / カード影）:
     - 上部: 店舗画像（`4:3`。未設定時はアンバーのビールジョッキアイコン + `surface` 面のフォールバック）
     - 下部:
       - 店名（明朝 `font-mincho` / 太字）
       - 所在地チップ（ピンアイコン + 都道府県 + 市町村）
     - タップで `/bars/[barId]` へ

4. **人気カテゴリ横スクロール**
   - ビールカテゴリ（IPA / ピルスナー / スタウト / ヴァイツェン / ペールエール）のタイルを横スクロール表示。タップで `/?cat=<カテゴリ>` の検索へ

**レイアウト（PC / 2a）**

- ヒーロー帯の右に検索カードを配置（ヒーロー左・検索右の2カラム）
  - 2カラムは上端揃え（`items-start`）。左ヒーローは右検索カードより背が低いため、中央揃えにすると見出しがファーストビュー中央まで沈み上部に空白が生まれる（Issue #498）。見出しは常にヘッダー直下から始める
- その下を2カラム: 左に地図（`sticky` で追従）/ 右に店舗一覧（2列グリッド）
- 最下部に人気カテゴリのグリッド

### 2-2. 店舗詳細ページ `/bars/[barId]`

基本情報タブ・メニュータブは Dark Taproom（3a〜4b）で再実装済み（#443）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。投稿/お店から/クーポン/イベントの各タブの Dark Taproom 化は本スコープ外（別 Issue）。

**レイアウト（Dark Taproom）**

- **ヒーロー（フルブリード写真・動画スライダー）**: 店舗メディア（`bar_images` 最大5枚。画像／動画が混在可。未設定時はビールジョッキアイコン + `surface-deep` フォールバック）を全幅で敷き、上下にグラデオーバーレイをかける。右上にアンバー丸ボタン群（戻る `ArrowLeft` / 共有 `Share2`（Web Share API、非対応時はURLコピー＋トースト） / お気に入り ★トグル）。複数メディア時はカルーセルドット（アクティブはアンバー）。下端に店名（明朝 `font-mincho`）＋所在地チップ（`MapPin` + 都道府県 + 市町村）を重ねる。
  - **スライダー挙動**: メディアが2件以上のとき、1枚あたり最大5秒でオートスライドし、末尾まで進んだら先頭へループする。切り替えはクロスフェード（`transition-opacity`）。動画（`media_type='video'`）はミュート・インライン自動再生し、5秒経過または再生終了（`onEnded`）のいずれか早い方で次へ送る。メディアが動画1件のみのときはオートスライドせず、その動画をループ再生する。カルーセルドットのタップで任意メディアへ手動切替でき、タップ時はオートスライドのタイマーがリセットされる。
- **タブナビ**: ヘッダー直下に `sticky`（`top-16`）で固定。アクティブはアンバー文字 + 下線（`aria-current="page"`）、非アクティブは `subtext`。
  - [基本情報] [メニュー] [タグ付けされた投稿] [お店からの投稿] [クーポン] [イベント]
  - **スクロールアフォーダンス（#500）**: モバイル幅ではタブ列が画面幅を超えるため横スクロールする。スクロール可能な方向にだけ端に `surface-deep` グラデのフェード（左右各 `w-8`）を重ね、続きがあることを示す。先頭では右のみ・末尾では左のみ・途中では両端に表示し、収まりきる PC 幅では両方非表示。フェードは `aria-hidden` の装飾でタブ操作には干渉しない。
- **投稿アクション**: モバイルはボトムナビ上に固定（アンバーグラデ「このお店に投稿する」）、PC はタブ配下にインラインで同ボタンを表示（`/posts/new?barId=[barId]` へ）。

#### タブ: 基本情報

- 店舗イメージはヒーロー（上記）に集約。基本情報タブ本文はテキスト情報のみを持つ。
- **レイアウト**: モバイルは1カラム縦積み。PC（`md:` 以上）は2カラム（左: PR文 / 右300px: 営業時間・情報カード・ウェブ・SNS）とし、情報カードが全幅に間延びしないよう右カラムを約300px幅に収める（design_handoff 3b）。左カラムのタグ・ミニマップは未実装（別Issue）。
- PR文（オーバーライン「About」`font-archivo` + 明朝リード `font-mincho`）
- 以下をカードで表示（`surface-raised` 面 + アンバー薄ボーダー、各行にアイコン + 区切り線）:

  - 営業時間（曜日別・複数時間帯対応。`Clock` アイコン付きカード）
    ```
    月曜日: 定休日
    火曜日: 17:00～23:00
    水曜日: 11:00～14:00、17:00～23:00
    木曜日: 11:00～14:00、17:00～23:00
    金曜日: 11:00～14:00、17:00～翌2:00
    土曜日: 11:00～翌2:00
    日曜日: 11:00～22:00
    ```
    - 定休日補足（`bars.regular_holiday`）: 隔週・不定休・祝日など曜日で表せない休業を、曜日別営業時間ブロックの直下に「定休日: {補足テキスト}」として併記表示する（曜日別営業時間の有無に関わらず表示。未設定時は非表示）
  - 住所（`MapPin`）
    - 住所テキストの下に地図アプリ経路案内の導線を2つ並べる（`Navigation` アイコン付きのアンバー枠ピルボタン。SNSボタンと同トーン）: 「マップで開く」（Appleマップ / `maps.apple.com`）と「Googleマップで開く」（`google.com/maps/dir`）
    - `bars.latitude` / `bars.longitude` が両方とも有効な数値なら座標を目的地に採用し、未登録（NULL・空文字・非数値）なら「都道府県+市町村+住所1+住所2」の住所文字列にフォールバックする。座標も住所も無い場合は導線を非表示にする
    - 各リンクは現在地からの経路案内を目的地=店舗で開く（Apple: `daddr=`、Google: `destination=`）。`https://` ユニバーサルリンクを新規タブ（`target="_blank"`）で開き、アプリ未インストール時はブラウザにフォールバックする
  - 電話番号（`Phone`。アンバー文字リンク。スマホはタップで発信）
  - 支払い方法（`Wallet`。未設定時は「-」）
  - 交通手段・アクセス（設定時のみ表示）
  - ウェブ・SNS: ホームページ（アンバー枠のピルボタン）と SNS（Instagram / X / Facebook / LINE）を「ウェブ・SNS」セクションにまとめてボタン表示
    - 各 SNS はアイコンのみのアンバー枠丸ボタン（URL / アカウント名テキストは併記しない）。タップで各SNSのURLへ遷移する
    - 未設定のSNS・URLは非表示。すべて未設定ならセクション自体を非表示

#### タブ: メニュー

- 見出し「Beers」（Archivo 太字 + 罫線 + `On Tap {件数}`）
  - ビールカード（横型・`surface-raised` 面）:
    - 左: ボトル写真（未設定時はビールジョッキアイコン + `surface-deep` フォールバック。Unsplash 等の外部ハードコード画像は使わない）
    - 右: ビール名（明朝）/ スタイル（`beerCategory.name`）・ABV（`beers.abv`。未設定時は非表示。アンバー文字）/ 説明文 / 醸造所・産地（`Factory` アイコン + 「醸造所名 / 産地」）/ サイズ・価格
- 見出し「Meals」（Archivo 太字 + 罫線）
  - 食事メニューカード（グリッド。写真上・情報下）:
    - 写真（未設定時は `UtensilsCrossed` アイコン + `surface-deep` フォールバック）
    - 料理名（明朝）/ 価格（アンバー文字）/ 説明文（任意）

#### タブ: タグ付けされた投稿
- 
- ユーザー投稿カードリスト
  - 上部: 投稿ユーザーアイコン + ニックネーム → `/users/[userId]`
  - 中央: 投稿写真（1〜4枚）
  - 下部:
    - 投稿本文
    - タグ一覧（チップ形式）→ タグ検索へ

#### タブ: お店からの投稿

- 記事カードリスト
  - サムネイル画像
  - 記事タイトル
  - 冒頭文（数行）
  - 投稿日
  - タップで `/articles/[articleId]`

#### タブ: クーポン

- クーポンカードリスト
  - 見出し
  - 内容
  - 取得条件
  - 有効期限
  - [クーポンを取得する] ボタン（タップで自分のクーポンに追加。`user_coupons` に INSERT）
    - 取得成功時: Sonner トースト「クーポンを取得しました」を表示し、ボタンを「取得済み」ラベルに切り替える
    - 取得済み: ボタンを「取得済み」ラベルで非活性表示（二重取得不可）
    - 有効期限外（開始前 / 終了後）: ボタンを「有効期限外」で非活性表示
    - 利用上限到達（`usage_limit` 非 NULL かつ `used_count >= usage_limit`）: ボタンを「利用上限に達しています」で非活性表示
    - 取得したクーポンはマイページ「持っているクーポン」タブに表示され、そこで利用（消し込み）できる

#### タブ: イベント

- イベントカードリスト
  - イベント画像（任意）
  - イベントタイトル
  - 説明文（任意）
  - 開始日時
  - 終了日時（任意）
- イベント0件時: 「イベントはまだ登録されていません。」メッセージ表示

---

## 3. 記事ページ

### 3-1. 記事ページ `/articles/[articleId]`

Dark Taproom（9a / 9b）で実装（#446）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。中央 660px 幅の1カラム。

**レイアウト**

- 上部: オーバーライン「ARTICLE」（Archivo・`uppercase`・アンバー）＋店舗名リンク（`subtext` → `/bars/[barId]`）＋記事タイトル（明朝 `font-mincho` 大見出し）
- メタ行（下線区切り）:
  - いいねボタン（トグル・塗り時は赤 `destructive`）＋いいね数
  - シェアボタン（`Share2`。Web Share API、非対応時は URL コピー＋トースト）
  - 投稿日（右寄せ・`subtext`）
- 画像（任意・最大3枚。`image_url` / `image_url_2` / `image_url_3` のうち設定済みのもののみ表示。`16:9`・角丸）
- 記事本文（明朝 `font-mincho`・ゆったりした行間 `leading-[2]`）
- 下部: 関連店舗カード（`bg-card`・「この記事のお店」見出し）
  - 店舗プレビュー画像サムネ（`bars.preview_image_url`。未設定時はビールジョッキアイコン + `surface-deep` フォールバック）＋店名（明朝）＋「店舗を見る」＋ `ChevronRight`。タップで `/bars/[barId]`

---

## 4. マイページ・ユーザー関連

### 4-1. マイページ `/mypage`

Dark Taproom（6a / 6b）で再実装済み（#445）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。

**レイアウト**

- 上部: プロフィール帯（グラデ背景 `surface-raised`〜`surface-deep`）
  - アバター（丸・アンバーボーダー。未設定時はニックネーム頭文字）+ ニックネーム（明朝 `font-mincho`）+ 一言（`bio`。任意）
  - サマリー: 投稿数 / フォロー数（→ `/mypage/following`）/ フォロワー数（→ `/mypage/followers`）
  - 「プロフィールを編集」ボタン（→ `/mypage/edit`）
- タブ構成（アクティブはアンバー文字 + 下線）:
  - [投稿] タブ（デフォルト）
    - 自分の投稿を**正方形3列グリッド**で表示（画像ありは先頭画像、画像なしは本文を敷いたタイル。タップで `/bars/[barId]`）
  - [持っているクーポン] タブ
    - 取得済みクーポンを**チケット型カード**で表示（左端に破線区切りの「Coupon」スタブ列 + 右に見出し / 内容 / 対象店舗 / 有効期限）
      - 使用済みは `opacity 0.6` + グレースタブ + 「使用済み」バッジ（利用ボタンは非表示）
      - [クーポンを利用する] ボタン（タップで消し込み。`user_coupons` を `is_used=true` に更新し `bar_coupons.used_count` を +1）
        - 利用成功時: Sonner トースト「クーポンを利用しました」を表示し、ボタンを「使用済み」ラベルに切り替える
        - 利用済み: 「使用済み」バッジを表示し、利用ボタンは非表示
        - 有効期限外（開始前 / 終了後）: ボタンを「有効期限外」で非活性表示
        - 利用上限到達（`usage_limit` 非 NULL かつ `used_count >= usage_limit`）: ボタンを「利用上限に達しています」で非活性表示

### 4-2. 自分のフォロー一覧 `/mypage/following`

- シンプルなリスト
  - ユーザーアイコン
  - ニックネーム
  - 一言（任意）
  - [フォロー中] ボタン（トグル）
  - タップで `/users/[userId]`

### 4-3. 自分のフォロワー一覧 `/mypage/followers`
---

## 5. 他ユーザー関連

### 5-1. 他人のアカウント詳細ページ `/users/[userId]`

**レイアウト**

- ユーザーアイコン + ニックネーム
- フォロー / フォロワー数（リンク）
- 自己紹介文（任意）
- [フォローする] / [フォロー中] ボタン
- セクションリンク:
  - [投稿を見る] → `/users/[userId]/posts`

### 5-2. 他人のフォロー一覧 `/users/[userId]/following`

- マイページと同じスタイルのユーザーリスト

### 5-3. 他人のフォロワー一覧 `/users/[userId]/followers`

- 同上

### 5-4. 他人の投稿一覧 `/users/[userId]/posts`

- 投稿カードリスト（自分の投稿一覧と同様）
  - 投稿者情報（このユーザー）
  - 店名
  - 写真
  - 本文
  - タグ
  - いいねボタン

---

## 6. タイムライン・お気に入り・投稿

### 6-1. タイムラインページ `/timeline`

Dark Taproom（5a / 5b）で実装済み（#444）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。取得データは `getTimelinePosts`（自分＋フォロー中ユーザーの投稿を時系列降順）。

**ページング（#523）**: `getTimelinePosts` は一度に最大 20 件（`TIMELINE_PAGE_SIZE`）までを返し、全件取得はしない。並び順は `createdAt desc, id desc` の複合キーで安定させ、次ページは投稿 id を基点にした複合カーソル（`{ id }`）で取得する。フィード末尾に続きがある場合のみ「もっと見る」ボタンを表示し、押下で `getTimelinePosts(cursor)` を呼んで続きを連結する（`components/post/timeline-post-list.tsx`）。続きが無くなるとボタンは消える。無限スクロールは採用しない（wireframe に記載が無く推測実装を避けるため）。

**レイアウト（Dark Taproom）**

- 上部: オーバーライン「Timeline」（`font-archivo` / `uppercase`）+ 明朝見出し「タイムライン」（`font-mincho` / `text-heading`）
- タブ: 「フォロー中」のみ描画（アクティブ = アンバー文字 + 下線 / `aria-current="page"`）。
  - **「みんな」タブ（全ユーザー投稿の横断表示）は現状の機能・データに存在しないため描画しない（推測実装禁止。追加は別 Issue）。同様にタグ・コメント・シェア・PC右サイドの「人気のタグ」も機能が無いため本スコープでは実装しない。**
- コンテンツ: 中央フィード（`bg-card` / 角丸 / アンバー薄ボーダーの投稿カード）。PC（`lg:` 以上）は右 240px サイドに「投稿促進カード」（アンバーグラデ背景 + `surface-deep` ボタン、`/posts/new` へ）を `sticky` 表示。

各投稿カード（`components/post/timeline-post-card.tsx`）:

- 上部:
  - ユーザーアバター（`user_profiles.profile_image_url`。未設定時はアンバーグラデ丸 + ニックネーム頭文字）+ ニックネーム → `/users/[userId]`
  - 投稿の相対時刻（`lib/utils.ts` の `formatRelativeTime`。「たった今 / n分前 / n時間前 / n日前」、7日以上は日付表記）
- 中央:
  - 写真（1〜4枚。1枚=`4:3` 単一、2枚=2列、3〜4枚=左1大 + 右縦積みの `2fr:1fr` 分割。5枚以上渡っても先頭4枚のみ描画）
  - 投稿本文
- 下部:
  - 店舗リンクピル（アンバー、`MapPin` アイコン + 店名。長い店名は省略）→ `/bars/[barId]`
  - いいねボタン（赤 `#e0483a` の塗り + カウント。既存 `togglePostLike` + 楽観更新。`components/post/like-button.tsx`）

### 6-2. お気に入りバーページ `/favorites/bars`

Dark Taproom（10a / 10b）で再実装済み（#445）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。

**レイアウト**

- 上部: タイトル「お気に入りバー」（明朝 `font-mincho` / `text-heading`）
- コンテンツ: 店舗カードグリッド（モバイル1列 / PC 3列。カードはトップページ店舗リストと同じ `bg-card` 基調）
  - 画像（`4:3`。未設定時はビールジョッキアイコン + `surface` 面のフォールバック）
  - 店名（明朝 `font-mincho`）
  - 所在地チップ（`MapPin` + 都道府県 + 市町村）
  - 右上に★解除トグル（`rgba(15,11,6,.6)` の丸背景 + アンバー塗り★）。押下で `removeFavoriteBar` を呼び、一覧から即時に消える
- 0件時: 破線枠 + ★アイコン + 「まだお気に入りがありません」+ 「お店をさがす」ボタン（`/` へ）の空状態

### 6-3. 投稿ページ `/posts/new`（店舗詳細から `?barId=[barId]` 付きで遷移）

Dark Taproom（7a / 7b）で実装済み（#444）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。書き込みは従来どおり `createPost` を流用（スキーマ変更なし）。

**レイアウト（Dark Taproom）**

- 上部バー: ✕ ボタン（`surface-raised` 丸・押下で `router.back()`）+ 明朝タイトル「投稿を作成」+ 「投稿する」プライマリ（アンバーグラデ `from-primary`〜`to-primary-strong`）。
- フォーム（PC は写真を左 380px・入力を右の 2 カラム、モバイルは縦積み）:

1. 写真（最大4枚）
   - サムネイルは正方形グリッド。各サムネ右上に削除 ✕（`surface-deep` 丸）
   - 追加は破線枠の追加ボタン（0枚時は `ImagePlus` + 「写真を追加」、1枚以上は `Plus`）。4枚に達すると追加ボタンは非表示
   - 上限超過時は「写真は最大4枚までです」を `destructive` 文字で表示

2. 投稿する店舗（固定表示）
   - 店舗詳細から遷移した店舗を**プルダウンで選び直させず**、`MapPin` アイコン + 店名 + 所在地を読み取り専用で表示する（`surface-raised` 面 + アンバー薄ボーダー）
   - `barId` は hidden で送信する

3. 投稿本文
   - テキストエリア（複数行・必須）。プレースホルダ文字は `#5f5138`

- 送信: 上部バーの「投稿する」で送信（投稿中はラベルが「投稿中...」に変わり非活性）。投稿完了後は店舗詳細 `/bars/[barId]` へ戻る

**振る舞い**

- `barId` が無い / 無効 / 非掲載店舗で直接アクセスした場合: 投稿フォームは表示せず、「店舗詳細から投稿してください」の案内と店舗検索（`/`）への導線（アンバーグラデボタン）を表示する

---

## 7. 閲覧履歴・通知

### 7-1. 閲覧履歴ページ `/history/bars`

Dark Taproom（12a / 12b）で再実装済み（#445）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。

**レイアウト**

- 上部: タイトル「閲覧履歴」（明朝 `font-mincho` / `text-heading`）
- コンテンツ: **Today / This Week でグルーピング**した軽量リスト
  - グループ見出し（`Today` / `This Week` / `それ以前`。Archivo・アンバー文字）。該当が無いグループは表示しない
  - 各リスト行（`bg-card` / 角丸 / アンバー薄ボーダー）:
    - サムネイル（56px。未設定時はビールジョッキアイコン + `surface-deep` フォールバック）
    - 店名（明朝 `font-mincho`）
    - 所在地（`MapPin` + 都道府県 + 市町村）
    - 右端にシェブロン（`ChevronRight`）。行タップで `/bars/[barId]`
  - グルーピングは取得済み `viewedAt` から純関数 `groupViewHistories`（`lib/history/group-view-histories.ts`）で算出する（`getViewHistories` の取得は変更なし）。境界は「Today = 当日 0:00 以降」「This Week = 今日を除く直近7日（6日前 0:00 まで）」「それ以前 = それより前」
- 0件時: 破線枠 + 時計アイコン + 「閲覧履歴はありません」の空状態

### 7-2. 通知一覧ページ `/notifications`

Dark Taproom（8a / 8b）で実装（#446）。配色・タイポは `design_handoff_user_screens/README.md` の Design Tokens が正。

**レイアウト**

- 上部: 見出し「通知」（明朝 `font-mincho`）。右に「すべて既読にする」ボタン（`Check` アイコン付き・アンバー枠ピル。未読が1件以上あるときのみ表示。押下で本人の未読を一括既読化）
- 通知リスト: 作成日時（暦日）で「今日 / 昨日 / それ以前」にグルーピング（該当のあるグループのみ、`subtext` の小見出しを出す）

各カード（`bg-card` / 角丸 / 未読はアンバー枠 + 薄アンバー背景 + 左3pxアンバーバー + 右アンバードット、既読は通常カード）:

- 左: 通知種別アイコン（丸背景・lucide）
  - いいね（`post_liked`）→ `Heart`（赤系 `destructive`）
  - フォロー（`followed`）→ `UserPlus`（アンバー）
  - 新着記事（`new_article`）→ `Newspaper`（アンバー）
  - その他 → `Bell`（`subtext`）
- 中央:
  - 通知タイトル（`heading`）
  - 補足テキスト（例: 「○○さんがあなたの投稿にいいねしました」）
  - 相対時刻（`subtext`）
- 右:
  - `ChevronRight` アイコン（タップで対象ページへ。未読ならタップで既読化）
    - 投稿 → タイムライン位置
    - ユーザー → `/users/[userId]`
    - 店舗 → `/bars/[barId]`
    - 記事 → `/articles/[articleId]`

---

## 8. レスポンシブのざっくり方針（メモ）

- モバイル:
  - ヘッダー固定・フッター固定・中央スクロール
- タブレット / PC:
  - フッターナビをサイドナビに変更するなどの拡張は後回し
  - まずはモバイルデザインをそのまま中央寄せで表示
- マップ + リストは PC では 2 カラム表示（将来的な改善案）

---

## 9. Claude Code への渡し方メモ

- 各ページごとのセクションを参照して、
  - 「まず `/login` と `/signup` をこの wireframe に沿って Next.js (App Router) + Tailwind で実装して」
  - 「`/bars/[barId]` の ‘トップ’ タブだけを先に作って」
- という形で、段階的に依頼する想定。
