{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["prisma/schema.prisma"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": [".next/**", "!.next/cache/**"]
		},
		"lint": {},
		"test": {},
		"format": {
			"cache": false
		}
	}
}
