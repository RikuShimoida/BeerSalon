# Wireframe Specification - BeerSalonAdmin（管理画面）

管理画面のテキストワイヤーフレーム。
モバイルファーストで設計し、PC では横幅に応じて余白や2カラムレイアウトにすることを想定。

---

## 0. 共通コンポーネント

### 0-1. ログイン前レイアウト

- ヘッダー: なし（または簡易ロゴのみ）
- コンテンツ: 中央寄せフォーム
- フッター: コピーライト簡易表記のみ（任意）

### 0-2. ログイン後レイアウト

- **共通ヘッダー（画面上部固定）**
  - 左: サービスロゴ（テキスト or 簡易ロゴ）「Beer Salon Admin」
  - 右: ログアウトボタン（テキストリンクでもOK）

- **共通サイドナビ / ハンバーガーメニュー**
  - モバイル: ハンバーガーメニュー（タップで展開）
  - PC: 左サイドバー（常時表示）
  - ナビ項目:
    - bar_owner の場合:
      - 店舗情報 → `/bars/[barId]`
      - メニュー管理 → `/bars/[barId]/menus`
      - 記事管理 → `/bars/[barId]/articles`
      - クーポン管理 → `/bars/[barId]/coupons`
      - イベント管理 → `/bars/[barId]/events`
    - admin の場合:
      - 店舗一覧 → `/bars`

- **コンテンツエリア**
  - ヘッダーの下、メインコンテンツ領域でスクロール

---

## 1. 認証画面

### 1-1. ログインページ `/login`

**レイアウト（モバイル）**

- 上部: サービスロゴ +「Beer Salon Admin」テキスト（中央揃え）
- 中央: ログインフォームカード
  - 店舗ID テキストボックス プレースホルダー: `fuji-beer-bar` 店舗識別用のスラッグ形式ID
  - パスワード テキストボックス 8文字以上
  - [ログイン] ボタン（幅いっぱい）
  - エラーメッセージエリア（バリデーション/認証失敗時のみ表示）

**ログイン後の遷移先**:
- admin: `/bars`（店舗管理ページ）
- bar_owner: `/bars/[barId]`（自店舗の店舗詳細ページ）

---

## 2. 店舗管理（admin専用）

### 2-1. 店舗管理ページ `/bars`

**レイアウト（モバイル）**

- ページ名: 店舗管理
  - [店舗を登録する] ボタン → `/bars/new`
  - 店舗カード一覧
    - 各カード:
      - プレビュー画像 画像表示 未設定時はプレースホルダー
      - 店舗名 テキスト（太字）→ `/bars/[barId]`（タップで遷移）

---

## 3. 店舗登録・編集

### 3-1. 店舗登録ページ `/bars/new`（admin専用）

**レイアウト（モバイル）**

- ページ名: 店舗登録
- ステッパーUI: Phase 1 → Phase 2

#### Phase 1（必須項目）

- 店舗ID テキストボックス 例: `fuji-beer-bar` スラッグ形式（半角英数字とハイフンのみ）。ログイン時に使用する `bar_manage_id`
- パスワード テキストボックス 8文字以上。店舗スタッフ全員で共有するログインパスワード
- 店舗管理者メールアドレス テキストボックス 例: `owner@fuji-beer.com` 請求書送付用
- 店舗管理者電話番号 テキストボックス 例: `090-1234-5678` トラブル時連絡用
- [次へ] ボタン → Phase 2 へ

#### Phase 2（全項目 optional）

- 店舗名 テキストボックス 例: `富士ビアバー`
- 説明（PR文） テキストエリア 店舗の紹介文
- 営業時間 曜日別入力UI 曜日ごとに開始/終了時刻を設定。1日に複数時間帯対応。定休日チェックボックスあり
- アクセス テキストボックス 例: `JR静岡駅北口から徒歩5分`
- 電話番号 テキストボックス 例: `054-123-4567` ユーザー画面で表示される店舗電話番号
- 住所（都道府県） プルダウン 静岡県固定（MVPは静岡県限定。`shizuoka-cities.ts` の `SHIZUOKA_PREFECTURE`）
- 住所（市区町村） プルダウン 静岡県内35市町村（`shizuoka-cities.ts` の `SHIZUOKA_MUNICIPALITIES`。全国展開時は自由入力 or 都道府県連動マスタへ拡張）
- 住所1 テキストボックス 番地等
- 住所2 テキストボックス 建物名・部屋番号（任意）
- ホームページ テキストボックス URL形式バリデーション
- Instagram テキストボックス ドメインバリデーション（instagram.com）
- X テキストボックス ドメインバリデーション（x.com または twitter.com）
- Facebook テキストボックス ドメインバリデーション（facebook.com）
- LINE テキストボックス https://で始まるURLバリデーション
- プレビュー画像 画像アップロード 1枚（店舗一覧ページ用サムネイル。最大5MB、JPEG/PNG/WebP）
- スライダー用メディア 画像・動画アップロード 最大5枚（店舗詳細ページのカルーセル用。画像5MB/動画50MB）
- [登録する] ボタン（プライマリ）→ 登録完了後 `/bars` へ遷移（admin）

### 3-2. 店舗詳細ページ `/bars/[barId]`

**レイアウト（モバイル）**

- ページ名: 店舗詳細
- 上部: 店舗名（大見出し）
  - [編集] ボタン → `/bars/[barId]/edit`
- 店舗情報カード:
  - 店舗画像 画像・動画表示（CSS scroll-snapカルーセル、ドットインジケーター、左右矢印ナビ）
    - bar_imagesがある場合: カルーセル表示
    - bar_imagesなし & preview_image_urlあり: 1枚表示
    - 両方なし: 画像エリア非表示
  - 説明（PR文） テキスト表示
  - 営業時間 曜日別表示（複数時間帯対応）
  - 定休日 テキスト表示
  - アクセス テキスト表示
  - 電話番号 テキスト表示（リンク）
  - 住所 テキスト表示（都道府県+市区町村+住所1+住所2）
  - ホームページ リンクテキスト
  - Instagram リンクテキスト
  - X リンクテキスト
  - Facebook リンクテキスト
  - LINE リンクテキスト

- 管理メニューセクション（カードリスト形式）:
  - メニュー管理 リンクカード → `/bars/[barId]/menus` bar_ownerのみ編集可、adminは参照のみ
  - 記事管理 リンクカード → `/bars/[barId]/articles` 同上
  - クーポン管理 リンクカード → `/bars/[barId]/coupons` 同上
  - イベント管理 リンクカード → `/bars/[barId]/events` 同上
  - 課金カード（active サブスク有無で出し分け）: 未サブスクなら「課金を開始する」→ Stripe Checkout（外部リダイレクト）、サブスク有りなら「支払い方法管理」→ Stripe Customer Portal（外部リダイレクト）

### 3-3. 店舗編集ページ `/bars/[barId]/edit`

**レイアウト（モバイル）**

- ページ名: 店舗情報編集
- フォーム項目（Phase 2 と同じ項目 + 以下の追加項目）:
  - 店舗名 テキストボックス
  - 説明（PR文） テキストエリア
  - 営業時間 曜日別入力UI
  - アクセス テキストボックス
  - 電話番号 テキストボックス
  - 住所（都道府県） プルダウン 静岡県固定（MVPは静岡県限定。`shizuoka-cities.ts` の `SHIZUOKA_PREFECTURE`）
  - 住所（市区町村） プルダウン 静岡県内35市町村（`shizuoka-cities.ts` の `SHIZUOKA_MUNICIPALITIES`。全国展開時は自由入力 or 都道府県連動マスタへ拡張）
  - 住所1 テキストボックス
  - 住所2 テキストボックス
  - ホームページ テキストボックス
  - Instagram テキストボックス ドメインバリデーション
  - X テキストボックス ドメインバリデーション
  - Facebook テキストボックス ドメインバリデーション
  - LINE テキストボックス https://で始まるURLバリデーション
  - 支払い方法 チェックボックス群（複数選択可）。`payment_methods`（`/api/payment-methods` から取得。`is_active=true` を `display_order` 順）を一覧表示し、店舗が対応する支払い方法を選択する。保存時に選択済み `payment_method_id` 群を PUT `/api/bars/[barId]` に `payment_method_ids` として送信し、`bar_payment_methods` を全削除→再登録で同期する（UNIQUE 制約 `bar_id + payment_method_id` を遵守）。既存の選択状態は GET `/api/bars/[barId]` の `payment_method_ids` で初期表示する。ユーザー画面の店舗詳細「基本情報」タブに「、」区切りで反映され、未選択時は「-」表示になる。Stripe Customer Portal（サブスク支払い）とは別概念
  - プレビュー画像 画像アップロード・変更・削除（1枚）
  - スライダー用メディア 画像・動画アップロード・削除・並び替え（最大5枚）

- 店舗ID変更セクション:
  - 現在の店舗ID テキスト表示（読み取り専用）
  - 新しい店舗ID テキストボックス スラッグ形式バリデーション
  - [店舗IDを変更する] ボタン 確認ダイアログ付き

- パスワード変更セクション:
  - 現在のパスワード テキストボックス
  - 新しいパスワード テキストボックス 8文字以上
  - 新しいパスワード（確認用） テキストボックス
  - [パスワードを変更する] ボタン

- 下部:
  - [保存する] ボタン（プライマリ）
  - [キャンセル] ボタン → `/bars/[barId]` へ戻る

---

## 4. メニュー管理

### 4-1. メニュー管理ページ `/bars/[barId]/menus`

**レイアウト（モバイル）**

- ページ名: メニュー管理
  - [メニューを登録する] ボタン → `/bars/[barId]/menus/new` bar_ownerのみ表示
  - ページ内アンカーリンク:
    - [ビールメニュー] → #beers セクションへスクロール
    - [食事メニュー] → #foods セクションへスクロール

- ビールメニューセクション（id: beers）:
  - 見出し「ビールメニュー」
  - ビールメニューカード一覧
    - 各カード → `/bars/[barId]/menus/beers/[menuId]`（タップで遷移）
      - 画像 サムネイル表示 未設定時はプレースホルダー
      - メニュー名 テキスト（太字）
      - 醸造所名 テキスト（小）

- 食事メニューセクション（id: foods）:
  - 見出し「食事メニュー」
  - 食事メニューカード一覧
    - 各カード → `/bars/[barId]/menus/foods/[menuId]`（タップで遷移）
      - 画像 サムネイル表示
      - 料理名 テキスト（太字）

### 4-2. メニュー登録ページ `/bars/[barId]/menus/new`

**レイアウト（モバイル）**

- ページ名: メニュー登録
- タブ切替:
  - [ビールメニュー] タブ（デフォルト）
  - [食事メニュー] タブ

#### ビールメニュータブ

- メニュー名 テキストボックス 例: `IPA Revolution` 必須
- 国 プルダウン countries テーブルから取得
- 産地 プルダウン 国連動。選択した国に紐づく regions テーブルから取得
- 醸造所 テキストボックス 例: `ベアードブルーイング`
- サイズ/価格 動的追加フォーム 上限なし。bar_beer_menu_sizes テーブルで管理
  - 各行:
    - サイズ名 テキストボックス 例: `パイント` 必須
    - 価格 テキストボックス（数値） 例: `900` 任意
    - [削除] ボタン（行削除）
  - [サイズ/価格を追加] ボタン（行追加）
- 説明 テキストエリア 任意
- 画像 画像アップロード finderが開く。1枚
- [登録する] ボタン 画面遷移なし。成功時トースト表示

#### 食事メニュータブ

- メニュー名 テキストボックス 例: `自家製ソーセージ盛り合わせ` 必須
- 価格（円） テキストボックス（数値） 例: `500` 任意
- 説明 テキストエリア 任意
- 画像 画像アップロード 1枚
- [登録する] ボタン 画面遷移なし。成功時トースト表示

### 4-3. ビールメニュー詳細ページ `/bars/[barId]/menus/beers/[menuId]`

**レイアウト（モバイル）**

- ページ名: ビールメニュー詳細
- コンテンツ:
  - 画像 画像表示
  - メニュー名 テキスト（大見出し）
  - 国 テキスト表示
  - 産地 テキスト表示
  - 醸造所 テキスト表示
  - サイズ/価格一覧 テーブル表示（サイズ名 | 価格）
  - 説明 テキスト表示
- 下部:
  - [編集] ボタン → `/bars/[barId]/menus/beers/[menuId]/edit` bar_ownerのみ表示
  - [削除] ボタン 確認ダイアログ付き。bar_ownerのみ表示

### 4-4. 食事メニュー詳細ページ `/bars/[barId]/menus/foods/[menuId]`

**レイアウト（モバイル）**

- ページ名: 食事メニュー詳細
- コンテンツ:
  - 画像 画像表示
  - メニュー名 テキスト（大見出し）
  - 価格 テキスト表示（`¥` + 3桁区切り。未設定時は非表示）
  - 説明 テキスト表示
- 下部:
  - [編集] ボタン → `/bars/[barId]/menus/foods/[menuId]/edit` bar_ownerのみ表示
  - [削除] ボタン 確認ダイアログ付き。bar_ownerのみ表示

### 4-5. メニュー編集ページ `/bars/[barId]/menus/beers/[menuId]/edit` / `/bars/[barId]/menus/foods/[menuId]/edit`

**レイアウト（モバイル）**

- ページ名: ビールメニュー編集 / 食事メニュー編集
- 入力項目は登録ページ（4-2）と同等
- 画像 画像アップロード（登録ページと同一方式。URL手入力ではない）
  - 既存画像をプレビュー表示
  - ファイル選択で差し替え（アップロード後に image_url を更新）
  - [画像を削除] ボタンで image_url を null 化（論理削除。Storage 上の旧ファイルは物理削除しない）
- [保存] ボタン 保存後にメニュー一覧へ戻る

---

## 5. 記事管理

### 5-1. 記事管理ページ `/bars/[barId]/articles`

**レイアウト（モバイル）**

- ページ名: 記事管理
  - [記事を投稿する] ボタン → `/bars/[barId]/articles/new` bar_ownerのみ表示
  - 記事カード一覧
    - 各カード → `/bars/[barId]/articles/[articleId]`（タップで遷移）
      - サムネイル画像 画像表示
      - 公開ステータスバッジ（下書き / 公開 / 予約公開）
      - 記事タイトル テキスト（太字）
      - 本文プレビュー テキスト（30字以降...で省略）

### 5-2. 記事登録ページ `/bars/[barId]/articles/new`

**レイアウト（モバイル）**

- ページ名: 記事投稿
- フォーム:
  - タイトル テキストボックス 必須
  - 本文 テキストエリア（複数行） 必須
  - 公開ステータス ラジオ（下書き / 公開 / 予約公開）。既定は「公開」
    - 「予約公開」選択時のみ「公開日時」（datetime-local）を表示。必須。未来日時のみ許容
  - 画像添付 画像アップロード 最大3枚。finderが開く
    - サムネイルプレビュー表示（添付済み画像）
    - 各画像に [削除] ボタン
- 下部:
  - 送信ボタン 画面遷移なし。成功時トースト表示。ステータスに応じて文言が変わる
    - 下書き → [下書き保存] / 公開 → [投稿する] / 予約公開 → [予約する]
  - published_at の決定: 公開は指定が無ければ現在時刻を自動記録、予約公開は指定日時、下書きは未設定（null）

### 5-3. 記事詳細ページ `/bars/[barId]/articles/[articleId]`

**レイアウト（モバイル）**

- ページ名: 記事詳細
- コンテンツ:
  - 公開ステータスバッジ（下書き / 公開 / 予約公開）
  - 記事タイトル テキスト（大見出し）
  - 日時 テキスト（yyyy-MM-dd HH:mm:ss形式）。予約公開時は「公開予定: 日時」と表示
  - 画像エリア 画像表示（最大3枚）
  - 本文 テキスト表示（段落）
- 下部:
  - [編集] ボタン → `/bars/[barId]/articles/[articleId]/edit` bar_ownerのみ表示
  - [削除] ボタン 確認ダイアログ付き。bar_ownerのみ表示。論理削除（deleted_at を記録）

---

## 6. クーポン管理

### 6-1. クーポン管理ページ `/bars/[barId]/coupons`

**レイアウト（モバイル）**

- ページ名: クーポン管理
  - [クーポンを登録する] ボタン → `/bars/[barId]/coupons/new` bar_ownerのみ表示
  - クーポンカード一覧
    - 各カード → `/bars/[barId]/coupons/[couponId]`（タップで遷移）
      - タイトル テキスト（太字）
      - 割引内容 テキスト（例: `10% OFF` / `500円引き`）
      - 有効期間 テキスト

### 6-2. クーポン登録ページ `/bars/[barId]/coupons/new`

**レイアウト（モバイル）**

- ページ名: クーポン登録
- フォーム:
  - タイトル テキストボックス 例: `初回来店クーポン` 必須
  - 内容 テキストエリア 例: `ファーストドリンク1杯無料`
  - 割引タイプ プルダウン `percentage`（%割引）/ `fixed_amount`（金額割引）必須
  - 割引値 テキストボックス（数値） 例: `10`（%の場合）/ `500`（円の場合）必須
  - 有効期間（開始） 日時ピッカー 任意
  - 有効期間（終了） 日時ピッカー 任意
- 下部:
  - [登録する] ボタン（プライマリ）

### 6-3. クーポン詳細ページ `/bars/[barId]/coupons/[couponId]`

**レイアウト（モバイル）**

- ページ名: クーポン詳細
- コンテンツ:
  - タイトル テキスト（大見出し）
  - 内容 テキスト表示
  - 割引タイプ テキスト表示（「%割引」/「金額割引」）
  - 割引値 テキスト表示
  - 有効期間 テキスト表示（開始〜終了）
- 下部:
  - [編集] ボタン → `/bars/[barId]/coupons/[couponId]/edit` bar_ownerのみ表示
  - [削除] ボタン 確認ダイアログ付き。bar_ownerのみ表示

---

## 7. イベント管理

### 7-1. イベント管理ページ `/bars/[barId]/events`

**レイアウト（モバイル）**

- ページ名: イベント管理
  - [イベントを登録する] ボタン → `/bars/[barId]/events/new` bar_ownerのみ表示
  - イベントカード一覧
    - 各カード → `/bars/[barId]/events/[eventId]`（タップで遷移）
      - 画像 サムネイル表示
      - タイトル テキスト（太字）
      - 開催日時 テキスト

### 7-2. イベント登録ページ `/bars/[barId]/events/new`

**レイアウト（モバイル）**

- ページ名: イベント登録
- フォーム:
  - タイトル テキストボックス 例: `クラフトビールナイト` 必須
  - 説明 テキストエリア 任意
  - 開始日時 日時ピッカー 必須
  - 終了日時 日時ピッカー 任意
  - 画像 画像アップロード 1枚
- 下部:
  - [登録する] ボタン（プライマリ）

### 7-3. イベント詳細ページ `/bars/[barId]/events/[eventId]`

**レイアウト（モバイル）**

- ページ名: イベント詳細
- コンテンツ:
  - 画像 画像表示
  - タイトル テキスト（大見出し）
  - 説明 テキスト表示
  - 開始日時 テキスト表示
  - 終了日時 テキスト表示
- 下部:
  - [編集] ボタン → `/bars/[barId]/events/[eventId]/edit` bar_ownerのみ表示
  - [削除] ボタン 確認ダイアログ付き。bar_ownerのみ表示

---

## 8. 課金・支払い方法管理

### 8-1. 課金開始（未サブスク時）

- 店舗詳細ページの課金カードは、当該店舗に active サブスクリプションが無い場合「課金を開始する」ボタンを表示する
- タップすると Stripe Checkout（mode: subscription・外部サイト）へ遷移し、決済完了でサブスクリプションが開始される
- 決済完了後、Stripe webhook（`customer.subscription.created`）が `bar_subscriptions` にレコードを作成する
- 既に active/trialing/past_due のサブスクがある店舗では、課金開始 API は 409 を返す（二重課金防止）

### 8-2. 支払い方法管理（サブスク有り時）

- active サブスクリプションがある場合、課金カードは「支払い方法管理」ボタンを表示する
- タップすると Stripe Customer Portal へ外部リダイレクトする
- 管理画面内に専用ページは設けない

---

## 9. 権限による表示制御まとめ

| 画面 | bar_owner | admin |
|------|-----------|-------|
| ログイン | 利用可 | 利用可 |
| 店舗管理（一覧） | 不可（自店舗詳細へ直接遷移） | 閲覧・店舗登録可 |
| 店舗詳細 | 自店舗のみ閲覧・編集可 | 全店舗閲覧・編集可 |
| 店舗登録 | 不可 | 登録可 |
| 店舗編集 | 自店舗のみ | 全店舗 |
| メニュー管理 | 閲覧・編集・登録・削除可 | 参照のみ（登録・編集・削除ボタン非表示） |
| 記事管理 | 閲覧・編集・登録・削除可 | 参照のみ |
| クーポン管理 | 閲覧・編集・登録・削除可 | 参照のみ |
| イベント管理 | 閲覧・編集・登録・削除可 | 参照のみ |
| 課金・支払い方法管理 | 未サブスクは Checkout、サブスク有りは Portal へ遷移 | 同左 |

---

## 10. 廃止した画面

以下の画面は廃止済み。実装が残っている場合は順次削除する。

- ダッシュボード `/`
- マスタ管理 `/admin/master/*`（beer-styles, breweries, food-categories, event-categories）
- 管理者ユーザー管理 `/admin/users/*`
- 課金情報 `/billing`

---

## 11. レスポンシブ方針

- モバイル:
  - ヘッダー固定、ハンバーガーメニューでナビゲーション
  - コンテンツエリアはスクロール
- タブレット / PC:
  - 左サイドバーにナビゲーション常時表示
  - コンテンツエリアは右側に配置
  - フォーム類は適度な max-width で中央寄せ
