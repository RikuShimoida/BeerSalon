# Routing Specification - Beer Salon

Beer Salon の画面遷移・URL 設計をまとめたドキュメント。  
フロントは Next.js(App Router) を前提とする。

---

## 1. 共通ルール

- ドメイン: `https://beersalon.com`
- 未ログインでアクセス可能なページ
  - `/login`
  - `/signup`
  - `/password/reset`
- それ以外のページはすべてログイン必須
  - 未ログイン時は `/login` へリダイレクト
- ログインユーザー向けページでは、原則として以下を共通レイアウトとして使用する
  - ヘッダー: ロゴ / マイページリンク / 通知ボタン / ログアウト
  - フッター: お気に入りバー / タイムライン / 投稿 / 閲覧履歴

---

## 2. ルート一覧（ユーザー画面）

### 2-1. 認証周り

#### 2-1-1. ログインページ

- Path: `/login`
- 認証: 不要（ログイン中にアクセスした場合は `/` へリダイレクト）
- 役割:
  - メールアドレス＋パスワードでログイン
  - 新規登録ページ / パスワード再設定への導線
- UI 要素:
  - メールアドレス（テキストボックス）
  - パスワード（テキストボックス）
  - 「新規登録はこちら」リンク → `/signup`
  - 「パスワードをお忘れの方」リンク → `/password/reset`

#### 2-1-2. 新規登録ページ

- Path: `/signup`
- 認証: 不要
- 役割:
  - メール＋パスワード or Google / X でのアカウント作成
  - 登録後、確認メールを送信し、メール内リンクからプロフィール入力へ遷移
- UI 要素:
  - メールアドレス入力（バリデーション: メール形式）
  - パスワード入力（バリデーション: 英大小文字＋数字＋記号 など）
  - 「Googleで登録」ボタン
  - 「Xで登録」ボタン
  - 「登録」ボタン → メール送信後に完了画面（または「メールを確認してください」メッセージ）

#### 2-1-3. プロフィール入力ページ

- Path: `/signup/profile`
- 認証: メールリンク経由でのみアクセス可（トークン / 一時セッション）
  - 未認証状態で直接アクセスした場合は `/signup` にリダイレクトされる
- 役割:
  - 初回プロフィール情報の入力
- UI 要素:
  - 姓 / 名
  - ニックネーム
  - 生年月日（プルダウン）
  - 性別（プルダウン）
  - お住まいの都道府県（プルダウン）
  - 「登録」ボタン → `/signup/confirm` へ

#### 2-1-4. 登録内容確認ページ

- Path: `/signup/confirm`
- 認証: `/signup/profile` と同様（一時セッション前提）
  - 未認証状態で直接アクセスした場合は `/signup` にリダイレクトされる
- 役割:
  - 入力済みプロフィールの確認
- UI 要素:
  - 姓 / 名 / ニックネーム / 生年月日 / 性別 / 都道府県（テキスト表示）
  - 「この内容で登録する」ボタン → 登録確定後 `/`（トップページ）へ遷移

#### 2-1-5. パスワード再設定申請ページ

- Path: `/password/forgot`
- 認証: 不要（未ログインユーザー向け）
- 役割:
  - 登録メールアドレスを入力し、パスワード再設定メールの送信を申請する
- UI 要素:
  - メールアドレス（テキストボックス）
  - 「再設定メールを送信」ボタン
  - 「ログインに戻る」リンク → `/login`
- 振る舞い:
  - 送信完了時はユーザー列挙を防ぐため、登録有無に関わらず常に同じ完了トーストを表示
    （例: 「該当アドレスが登録されていればメールを送信しました」）
  - クエリ `?error=invalid_token` の場合: トークン不正のエラートーストを表示
  - クエリ `?error=session_expired` の場合: 有効期限切れのエラートーストを表示

#### 2-1-6. 認証コールバック

- Path: `/auth/callback`
- 認証: 不要（メールリンク経由）
- 役割:
  - Supabase Auth のメールリンクを受け取り、`type` パラメータに応じてリダイレクト先を切り替える
- 主要なフロー:
  - `type=signup`（または `code` パラメータ）: 新規登録完了処理 → `/signup/profile` へ遷移
  - `type=recovery`: `verifyOtp` でリカバリーセッションを発行 → `next` クエリ（既定 `/password/reset`）へ遷移
  - リカバリー時に検証エラー: `/password/forgot?error=invalid_token` へリダイレクト

#### 2-1-7. パスワード再設定ページ

- Path: `/password/reset`
- 認証: Supabase Auth のリカバリーセッションが必要（`/auth/callback?type=recovery` 経由でのみ到達）
  - リカバリーセッションが無い状態で直接アクセスした場合は `/password/forgot?error=session_expired` にリダイレクトされる
- 役割:
  - 新しいパスワードを設定する
- UI 要素:
  - 新しいパスワード（テキストボックス、強度ルールあり）
  - 新しいパスワード（確認用）（テキストボックス、一致チェックあり）
  - 「パスワードを変更する」ボタン
- 振る舞い:
  - 更新成功時は `signOut()` して `/login?reset=success` にリダイレクト
  - `/login?reset=success` 時は「パスワードを再設定しました。新しいパスワードでログインしてください」トーストを表示

---

### 2-2. 共通レイアウト構成要素

#### 2-2-1. 共通ヘッダー（ログイン後のみ表示）

- 各ページ共通レイアウトの一部として実装
- 含まれる要素:
  - サービスロゴ（クリックで `/` へ）
  - マイページリンク → `/mypage`
  - 通知ボタン → `/notifications`
  - ログアウト（押下でセッション破棄 → `/login`）

#### 2-2-2. 共通フッター（ログイン後のみ表示）

- 各ページ共通レイアウトの一部として実装
- 含まれる要素:
  - 「お気に入りバー」 → `/favorites/bars`
  - 「タイムライン」 → `/timeline`
  - 「投稿」 → `/posts/new`
  - 「閲覧履歴」 → `/history/bars`

---

### 2-3. 検索・店舗関連

#### 2-3-1. トップページ（検索ページ）

- Path: `/`
- 認証: 必須
- 役割:
  - 店舗検索のメイン画面
  - Google Maps と店舗一覧コンポーネントを同一画面で表示
- 主な UI:
  - 検索バー
    - テキスト検索: 店名・説明・タグなど全文検索
    - 市町村フィルタ（選択すると検索バーに反映 or クエリパラメータ更新）
    - ビールカテゴリフィルタ（同上）
    - 検索ボタン（虫眼鏡） → ページ遷移せずに下部の店舗一覧コンポーネントを更新
  - Google Maps 表示
    - デフォルトは現在地近辺
    - 検索条件変更でマップ中心位置も更新
  - 店舗一覧コンポーネント（同一ページ内に埋め込み）

- 推奨クエリパラメータ:
  - `q`: フリーワード
  - `city`: 市町村名
  - `cat`: ビールカテゴリID など

#### 2-3-2. 店舗一覧コンポーネント（埋め込み）

- Path: `/(同一ページ内コンポーネント)`
- 別ページにはせず、`/` の下部コンポーネントとして実装
- 役割:
  - 検索結果の店舗リスト表示
- 各行要素:
  - 画像（最大4枚のサムネイル）: 外観 / 店内 / ビール / 料理
  - 店舗名
  - 都道府県
  - 市町村
  - クリック or タップで `/bars/[barId]` へ

#### 2-3-3. 店舗詳細ページ

- Path: `/bars/[barId]`
- 認証: 必須
- 役割:
  - 店舗の詳細情報、メニュー、クーポン、関連投稿をタブで表示

- 共通エリア
  - 店舗名
  - お気に入りボタン
  - このお店について投稿する → `/posts/new`
- タブ構成:

1. トップタブ
   - PR 文
   - 営業時間
   - 定休日
   - 交通手段・アクセス
   - 電話番号（スマホならタップで電話アプリ起動）
   - 住所
   - ホームページリンク
   - SNSリンク（Instagram、X、Facebook、LINE）

2. メニュータブ
   - Beers（上半分）
     - メニュー名
     - 説明文
     - 写真
     - 醸造所
     - 産地
   - Meals（下半分）
     - 料理名・説明・写真など

3. タグ付けされた投稿タブ
   - この店に紐づくユーザー投稿一覧
   - 投稿者アカウント → `/users/[userId]`
   - 投稿写真
   - 投稿本文
   - タグ → タグ検索に遷移（実装方針により `/` の検索パラメータ更新など）

4. お店からの投稿タブ
   - 店舗オーナーが投稿したブログ・お知らせ記事一覧
   - 各記事 → `/articles/[articleId]`

5. クーポンタブ
   - 店舗が発行しているクーポン一覧
   - 見出し / 内容文 / 取得条件表示

6. イベントタブ
   - 店舗が開催するイベント一覧
   - イベントタイトル / 説明文 / 開始日時 / 終了日時 / 画像

---

### 2-4. 記事・コンテンツ

#### 2-4-1. 記事ページ

- Path: `/articles/[articleId]`
- 認証: 必須
- 役割:
  - 店舗オーナーの記事、新R25形式のインタビュー記事等の詳細表示
- UI:
  - 記事タイトル
  - 記事本文
  - 必要に応じて店舗へのリンク（`/bars/[barId]`）

---

### 2-5. ユーザー関連

#### 2-5-1. マイページ

- Path: `/mypage`
- 認証: 必須
- 役割:
  - 自分のプロフィール周りのハブ画面
  - 投稿とクーポンをタブで切り替えて表示
- UI:
  - フォロー数（リンク）→ `/mypage/following`
  - フォロワー数（リンク）→ `/mypage/followers`
  - タブ構成:
    - 投稿タブ: 自分の投稿一覧を表示
    - 持っているクーポンタブ: 取得済みクーポン一覧を表示

#### 2-5-2. 自分のフォロー一覧

- Path: `/mypage/following`
- 認証: 必須
- 役割:
  - 自分がフォローしているユーザー一覧
- 各行:
  - ユーザー情報 → `/users/[userId]`

#### 2-5-3. 自分のフォロワー一覧

- Path: `/mypage/followers`
- 認証: 必須
- 役割:
  - 自分をフォローしているユーザー一覧
- 各行:
  - ユーザー情報 → `/users/[userId]`


---

### 2-6. 他ユーザー関連

#### 2-6-1. 他人のアカウント詳細ページ

- Path: `/users/[userId]`
- 認証: 必須
- 役割:
  - 他ユーザーのプロフィール・投稿への入口
- UI:
  - フォロー数 → `/users/[userId]/following`
  - フォロワー数 → `/users/[userId]/followers`
  - 投稿一覧 → `/users/[userId]/posts`
  - フォローボタン / フォロー解除ボタン

#### 2-6-2. 他人のフォロー一覧

- Path: `/users/[userId]/following`
- 認証: 必須

#### 2-6-3. 他人のフォロワー一覧

- Path: `/users/[userId]/followers`
- 認証: 必須

#### 2-6-4. 他人の投稿一覧

- Path: `/users/[userId]/posts`
- 認証: 必須
- 役割:
  - 指定ユーザーの投稿一覧
- 各行:
  - 投稿者 → `/users/[userId]`
  - 投稿写真
  - 投稿本文
  - タグ → 検索へ

---

### 2-7. タイムライン・投稿

#### 2-7-1. タイムラインページ

- Path: `/timeline`
- 認証: 必須
- 役割:
  - 自分がフォローしているユーザーの投稿を時系列で表示
- 各投稿:
  - 投稿者 → `/users/[userId]`
  - 投稿写真
  - 投稿本文
  - タグ → 検索（`/` のクエリ更新など）

#### 2-7-2. お気に入りバー一覧ページ

- Path: `/favorites/bars`
- 認証: 必須
- 役割:
  - ユーザーがお気に入り登録したバーの一覧
- 各行:
  - 店舗画像（最大4枚）
  - 店名
  - 都道府県
  - 市町村
  - クリックで `/bars/[barId]`

#### 2-7-3. 投稿作成ページ

- Path: `/posts/new`
- 認証: 必須
- 役割:
  - バーに紐づく投稿を新規作成
- UI:
  - 投稿本文（テキストエリア）
  - 写真アップロード（最大4枚）
  - 店名タグ（必須：バー選択 UI or 検索 & 選択）
  - 投稿ボタン（投稿完了後、店舗詳細 or タイムラインへ戻る）

---

### 2-8. 閲覧履歴・通知

#### 2-8-1. 閲覧履歴ページ

- Path: `/history/bars`
- 認証: 必須
- 役割:
  - 最近閲覧したバーの履歴を一覧表示
- 各行:
  - 店舗画像（最大4枚）
  - 店名
  - 都道府県
  - 市町村
  - クリックで `/bars/[barId]`

#### 2-8-2. 通知一覧ページ

- Path: `/notifications`
- 認証: 必須
- 役割:
  - 以下のような通知を一覧表示
    - 自分の投稿に「いいね」やコメントがついた
    - お気に入りのお店が新しい記事を投稿した
    - フォローされた など
- 各行:
  - 通知種別
  - 簡単なテキスト
  - 関連リンク（投稿・ユーザー・店舗など）

---

---

## 3. ルート一覧（管理画面 - BeerSalonAdmin）

管理画面は `apps/admin` で独立したNext.jsアプリとして実装。ポート3001で起動。

### 3-0. 共通ルール

- ドメイン: `https://admin.beersalon.com`（想定）
- 未ログインでアクセス可能なページ: `/login` のみ
- それ以外はすべてログイン必須（未ログイン時は `/login` へリダイレクト）
- 認証方式: カスタムJWT認証（jose + bcryptjs）。Supabase Auth は不使用
- ログイン後の共通レイアウト:
  - ヘッダー: ロゴ「Beer Salon Admin」/ ログアウト
  - サイドナビ（PC）/ ハンバーガーメニュー（モバイル）

### 3-1. 認証

#### 3-1-1. ログインページ

- Path: `/login`
- 認証: 不要（ログイン中にアクセスした場合は権限に応じたページへリダイレクト）
- 役割:
  - `bar_manage_id`（店舗ID）＋パスワードでログイン
- UI 要素:
  - 店舗ID（テキストボックス）
  - パスワード（テキストボックス、8文字以上）
  - ログインボタン
- ログイン後遷移先:
  - `admin` → `/bars`（店舗管理ページ）
  - `bar_owner` → `/bars/[barId]`（自店舗の詳細ページ）

### 3-2. 店舗管理（admin専用）

#### 3-2-1. 店舗管理ページ

- Path: `/bars`
- 認証: 必須（admin のみ）
- 役割:
  - 登録済み店舗の一覧表示
  - 新規店舗登録への導線
- UI 要素:
  - 店舗登録ボタン → `/bars/new`
  - 店舗カード一覧（店舗名、プレビュー画像）
  - 各カード → `/bars/[barId]`

#### 3-2-2. 店舗登録ページ

- Path: `/bars/new`
- 認証: 必須（admin のみ）
- 役割:
  - 新規店舗とそのログインアカウント（admin_users）を同時に作成
- UI:
  - ステッパーUI（Phase 1 → Phase 2）
  - Phase 1（必須項目）: 店舗ID、パスワード、管理者メールアドレス、管理者電話番号
  - Phase 2（任意項目）: 店舗名、説明、営業時間、アクセス、電話番号、住所、HP、SNS、店舗画像

### 3-3. 店舗詳細・編集

#### 3-3-1. 店舗詳細ページ

- Path: `/bars/[barId]`
- 認証: 必須（bar_owner: 自店舗のみ、admin: 全店舗）
- 役割:
  - 店舗基本情報の表示
  - 各管理機能（メニュー・記事・クーポン・イベント・支払い方法）への導線
- UI 要素:
  - 店舗情報表示（名前、説明、営業時間、アクセス、電話番号、住所、HP、SNS、画像）
  - 編集ボタン → `/bars/[barId]/edit`
  - 管理メニューリンク:
    - メニュー管理 → `/bars/[barId]/menus`
    - 記事管理 → `/bars/[barId]/articles`
    - クーポン管理 → `/bars/[barId]/coupons`
    - イベント管理 → `/bars/[barId]/events`
    - 支払い方法管理 → Stripe Customer Portal（外部リダイレクト）

#### 3-3-2. 店舗編集ページ

- Path: `/bars/[barId]/edit`
- 認証: 必須（bar_owner: 自店舗のみ、admin: 全店舗）
- 役割:
  - 店舗基本情報の編集
  - 店舗ID変更、パスワード変更
- UI 要素:
  - 店舗情報編集フォーム（Phase 2 と同じ項目）
  - 店舗ID変更セクション
  - パスワード変更セクション

### 3-4. メニュー管理

#### 3-4-1. メニュー管理ページ

- Path: `/bars/[barId]/menus`
- 認証: 必須（bar_owner: 編集可、admin: 参照のみ）
- 役割:
  - ビールメニュー/食事メニューの一覧表示
- UI 要素:
  - メニュー登録ボタン → `/bars/[barId]/menus/new`（bar_ownerのみ）
  - ビールメニュー / 食事メニューのアンカーリンク
  - ビールメニューカード一覧（各カード → `/bars/[barId]/menus/beers/[menuId]`）
  - 食事メニューカード一覧（各カード → `/bars/[barId]/menus/foods/[menuId]`）

#### 3-4-2. メニュー登録ページ

- Path: `/bars/[barId]/menus/new`
- 認証: 必須（bar_owner のみ）
- 役割:
  - ビールメニュー / 食事メニューの新規登録
- UI 要素:
  - タブ切替（ビール / 食事）
  - ビールメニュータブ: メニュー名、国、産地（国連動）、醸造所、サイズ/価格（動的追加）、説明、画像
  - 食事メニュータブ: メニュー名、説明、画像
  - 登録ボタン（画面遷移なし）

#### 3-4-3. ビールメニュー詳細ページ

- Path: `/bars/[barId]/menus/beers/[menuId]`
- 認証: 必須
- 役割:
  - ビールメニューの詳細表示
- UI 要素:
  - メニュー名、国、産地、醸造所、サイズ/価格一覧、説明、画像
  - 編集ボタン → `/bars/[barId]/menus/beers/[menuId]/edit`（bar_ownerのみ）
  - 削除ボタン（bar_ownerのみ）

#### 3-4-4. 食事メニュー詳細ページ

- Path: `/bars/[barId]/menus/foods/[menuId]`
- 認証: 必須
- 役割:
  - 食事メニューの詳細表示
- UI 要素:
  - メニュー名、説明、画像
  - 編集ボタン → `/bars/[barId]/menus/foods/[menuId]/edit`（bar_ownerのみ）
  - 削除ボタン（bar_ownerのみ）

### 3-5. 記事管理

#### 3-5-1. 記事管理ページ

- Path: `/bars/[barId]/articles`
- 認証: 必須（bar_owner: 編集可、admin: 参照のみ）
- 役割:
  - 店舗の記事一覧表示
- UI 要素:
  - 記事登録ボタン → `/bars/[barId]/articles/new`（bar_ownerのみ）
  - 記事カード一覧（記事名、プレビュー画像、本文プレビュー30字以降...）
  - 各カード → `/bars/[barId]/articles/[articleId]`

#### 3-5-2. 記事登録ページ

- Path: `/bars/[barId]/articles/new`
- 認証: 必須（bar_owner のみ）
- 役割:
  - 記事の新規投稿
- UI 要素:
  - タイトル、本文、画像添付（最大3枚）
  - 投稿ボタン（画面遷移なし、投稿日時自動記録 JSTでyyyy-MM-dd HH:mm:ss）

#### 3-5-3. 記事詳細ページ

- Path: `/bars/[barId]/articles/[articleId]`
- 認証: 必須
- 役割:
  - 記事の詳細表示
- UI 要素:
  - タイトル、本文、画像（最大3枚）、投稿日時
  - 編集ボタン → `/bars/[barId]/articles/[articleId]/edit`（bar_ownerのみ）
  - 削除ボタン（bar_ownerのみ、論理削除）

### 3-6. クーポン管理

#### 3-6-1. クーポン管理ページ

- Path: `/bars/[barId]/coupons`
- 認証: 必須（bar_owner: 編集可、admin: 参照のみ）
- 役割:
  - クーポン一覧表示
- UI 要素:
  - クーポン登録ボタン → `/bars/[barId]/coupons/new`（bar_ownerのみ）
  - クーポンカード一覧
  - 各カード → `/bars/[barId]/coupons/[couponId]`

#### 3-6-2. クーポン登録ページ

- Path: `/bars/[barId]/coupons/new`
- 認証: 必須（bar_owner のみ）
- 役割:
  - クーポンの新規作成
- UI 要素:
  - タイトル、内容、割引タイプ、割引値、有効期間（開始/終了）
  - 登録ボタン

#### 3-6-3. クーポン詳細ページ

- Path: `/bars/[barId]/coupons/[couponId]`
- 認証: 必須
- 役割:
  - クーポンの詳細表示
- UI 要素:
  - タイトル、内容、割引タイプ、割引値、有効期間
  - 編集ボタン → `/bars/[barId]/coupons/[couponId]/edit`（bar_ownerのみ）
  - 削除ボタン（bar_ownerのみ）

### 3-7. イベント管理

#### 3-7-1. イベント管理ページ

- Path: `/bars/[barId]/events`
- 認証: 必須（bar_owner: 編集可、admin: 参照のみ）
- 役割:
  - イベント一覧表示
- UI 要素:
  - イベント登録ボタン → `/bars/[barId]/events/new`（bar_ownerのみ）
  - イベントカード一覧
  - 各カード → `/bars/[barId]/events/[eventId]`

#### 3-7-2. イベント登録ページ

- Path: `/bars/[barId]/events/new`
- 認証: 必須（bar_owner のみ）
- 役割:
  - イベントの新規作成
- UI 要素:
  - タイトル、説明、開始日時、終了日時、画像
  - 登録ボタン

#### 3-7-3. イベント詳細ページ

- Path: `/bars/[barId]/events/[eventId]`
- 認証: 必須
- 役割:
  - イベントの詳細表示
- UI 要素:
  - タイトル、説明、開始日時、終了日時、画像
  - 編集ボタン → `/bars/[barId]/events/[eventId]/edit`（bar_ownerのみ）
  - 削除ボタン（bar_ownerのみ）

### 3-8. 支払い方法管理

- Stripe Customer Portal への外部リダイレクト
- 管理画面内に専用ページは設けない

### 3-9. 廃止したルート

以下のルートは廃止済み。実装が残っている場合は順次削除する。

- `/`（ダッシュボード）
- `/admin/master/*`（マスタ管理: beer-styles, breweries, food-categories, event-categories）
- `/admin/users/*`（管理者ユーザー管理）
- `/billing`（課金情報）

---

## 4. 今後の拡張候補（メモ）

- タグ検索専用ページ `/tags/[tagName]`
- 言語切り替え（日本語 / 英語）用のクエリ or サブパス

